module Players
  class Computer < Player

    def move(input)
      #first priority is stop 3 in a row
      #selects center first
      #selects corner next
      #selects to finish row
      #
    end

  end
end
